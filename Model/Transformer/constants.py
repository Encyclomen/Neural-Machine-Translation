# 全局常量
enc_unk = 0  # the padding index in encoder's input batch
dec_unk = 0  # the padding index in decoder's input batch
enc_pad = 1  # the padding index in encoder's output batch
dec_pad = 1  # the padding index in decoder's output batch
enc_sos = 2  # the start-of-sentence tag's index in encoder's output batch
dec_sos = 2  # the start-of-sentence tag's index in decoder's output batch
enc_eos = 3  # the end-of-sentence tag's index in encoder's output batch
dec_eos = 3  # the end-of-sentence tag's index in decoder's output batch

UNK = '<unk>'
PAD = '<pad>'
SOS = '<sos>'
EOS = '<eos>'